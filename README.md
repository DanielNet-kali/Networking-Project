# Networking-Project
This is a Custom(manual) Static routing.
The primary purpose of custom (or manual) static routing is to explicitly define a fixed path that packets must take to reach a specific destination network (or to serve as a default route), giving the administrator precise control over forwarding decisions
